extends layout/default.pug
include mixins/box-head.pug 

block main 
    +box-head("Trang danh sách sản phẩm", "Đây là trang danh sách sản phẩm")
    

    ul 
        each item in product
            li #{item.title} - #{item.price}
